
@inject SignInManager<UserModel> SignInManager
@inject UserManager<UserModel> UserManager

@model TeacherThesesVM

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    @if (SignInManager.IsSignedIn(User) && User.IsInRole("Teacher"))
    {
        <a asp-action="Create">Create New</a>
    }
</p>

<form asp-controller="TeacherThesis" asp-action="Index" method="get">
    <p>
    Title: <input type="text" asp-for="SearchString"/>
    
    Status: <select asp-for="SearchStatus" asp-items="Html.GetEnumSelectList<StatusEnum>()">
        <option>Any</option>
    </select>
    </p>
    <input type="submit" value="Filter" />
</form>

<table class="table">
    
    <thead>
        <tr>
            <th onclick="sortTable(0)">
                @Html.DisplayNameFor(model => model.ThesisList![0].Thesis.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThesisList![0].Thesis.DefendDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThesisList![0].Thesis.Grade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThesisList![0].Thesis.Tags)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThesisList![0].Thesis.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThesisList![0].Student)
            </th>
            <th></th>
        </tr>
    </thead>
    
    <tbody>
        @foreach (var item in Model.ThesisList!)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Thesis.Title)
            </td>
            <td>
                    @if (item.Thesis.DefendDate != null)
                {
                        @Html.DisplayFor(modelItem => item.Thesis.DefendDate)
                    }
                    else
                    {
                        if (item.Thesis.Status == StatusEnum.Done)
                        {
                            <a asp-action="SetDefense" asp-route-id="@item.ThesisID" method="post"></a>
                        }
                        else
                        {
                            
                        }
                    }
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Thesis.Grade)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Thesis.Tags)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Thesis.Status)
            </td>
            
            @if(item.Student is not null)
            {
            <td>
            @Html.DisplayFor(modelItem => item.Student!.FullName)
            </td>
            }
            else
            {
             <td>
                 <a asp-action="ViewRequests" asp-controller="TeacherTheses" asp-route-id="@item.ThesisID" > View thesis requests</a>
             </td>           
            }
           
			@if (SignInManager.IsSignedIn(User))
            {
                @if (User.IsInRole("Teacher"))
                {
                    <td>
                            <a asp-action="Edit" asp-controller="TeacherTheses" asp-route-id="@item.ThesisID">Edit</a> |
                    <a asp-action="Details" asp-controller="Thesis" asp-route-id="@item.ThesisID">Details</a>
                    </td>
                }
            }
            else
            {
                <td>
                    <a>
                        
                    </a>
                </td>
            }
            </tr>
        }
    </tbody>

</table>
